# Contract — 12_Management.md

<!-- gdd-forge chapter contract · one file per chapter · index: ../chapter-contracts.md · consistency rules: ../consistency-rules.md -->

## Conventions
- `brief:D-xx` — a decision field from `brief-schema.md`
- `ch N` — the finished markdown of chapter N (path given by the orchestrator)
- `tmpl` — the template skeleton you must follow (headings fixed, guidance comments removed in output); path given in your dispatch
- `check` — the checklist you self-check against before reporting; path given in your dispatch
- `data` — reference knowledge; the only allowed source of external facts
- **Depth rule** — how this chapter shrinks or grows based on triggers. An N/A chapter still opens with one short paragraph stating why it is minimal and which decision caused it, followed by any residual content. **A section removed by a Depth rule keeps its numbered heading**, followed by exactly one line: `N/A — <reason, ≤ 12 words>.` (dispatch rule 10) — e.g. `N/A — no narrative framing.` No `(D-xx)` tail. Never delete a numbered heading: downstream dispatches are sliced by heading number and a missing heading halts the pipeline.
- Output language for the body = `brief:D-12`. Headings keep the English chapter-number prefix (e.g. `# 4. Gameplay and Mechanics`) in every language.

## Owner
`gdd-producer`

## Consumes
ch 3 (§3.3, §3.9), ch 4 (§4.2, §4.7, §4.11), ch 6 (§6.7), ch 9 (§9.10), ch 10 (§10.9), ch 11 (§11.5, §11.7); `brief:D-06, D-09, D-10, D-15, D-24, D-30, D-31, D-36, D-38, D-40, D-44`

## Template / checklists / data
- tmpl: `12-management.md`
- check: `ch12-management.md`, `ch12-monetization.md`, `ch12-liveops.md`
- data: the `data/monetization/` slices D-06 selects (see `data/monetization/_index.md`)

## Sections
- 12.1 Development Roadmap (phases: concept → prototype → vertical slice → alpha → beta → gold → live; per phase: goal, exit criteria, chapters/features it proves)
- 12.2 Milestone Plan (table; durations `(est.)` scaled to D-10 if resolved, otherwise relative "T+n" with Open Decision)
- 12.3 Team & Roles (from D-10; roles derived from feature list)
- 12.4 Budget Drivers (qualitative cost centres; no currency figures unless in brief)
- 12.5 Risk Register (table: risk · source chapter · likelihood · impact · mitigation · owner)
- 12.6 Monetisation Plan (only if D-06 ≠ Premium: model, mechanisms strictly from D-30, ethics limits D-31, KPI definitions)
- 12.7 LiveOps Plan (only if D-24 ≠ None: cadence D-36, content types, events, community)
- 12.8 Market Positioning (comparables from D-13; qualitative only — no market-size numbers)
- 12.9 Localisation & Release Plan (D-38, D-40 rating path)
- 12.10 QA & Playtest Plan (what each milestone tests)
- 12.11 Post-launch & Sunset considerations
- Open Decisions box.

## Depth rule
D-06 = Premium → 12.6 one paragraph on pricing tier as Open Decision. D-24 = None → 12.7 one line. D-10 = UNDECIDED → 12.2 relative timeline only.
