# Contract — 11_Secondary Software.md

<!-- gdd-forge chapter contract · one file per chapter · index: ../chapter-contracts.md · consistency rules: ../consistency-rules.md -->

## Conventions
- `brief:D-xx` — a decision field from `brief-schema.md`
- `ch N` — the finished markdown of chapter N (path given by the orchestrator)
- `tmpl` — the template skeleton you must follow (headings fixed, guidance comments removed in output); path given in your dispatch
- `check` — the checklist you self-check against before reporting; path given in your dispatch
- `data` — reference knowledge; the only allowed source of external facts
- **Depth rule** — how this chapter shrinks or grows based on triggers. An N/A chapter still opens with one short paragraph stating why it is minimal and which decision caused it, followed by any residual content. **A section removed by a Depth rule keeps its numbered heading**, followed by exactly one line: `N/A — <reason, ≤ 12 words>.` (dispatch rule 10) — e.g. `N/A — no narrative framing.` No `(D-xx)` tail. Never delete a numbered heading: downstream dispatches are sliced by heading number and a missing heading halts the pipeline.
- Output language for the body = `brief:D-12`. Headings keep the English chapter-number prefix (e.g. `# 4. Gameplay and Mechanics`) in every language.

## Owner
`gdd-tech-designer` (written in the same run as ch 9, after it)

## Consumes
ch 9, ch 8 (§8.10), ch 6 (§6.6), ch 10 (§10.8); `brief:D-11, D-42`

## Template / checklists / data
- tmpl: `11-secondary-software.md`
- check: `ch11-tooling.md`

## Sections
- 11.1 Tooling Overview & Principles
- 11.2 Editor & Engine Tooling (level editor needs, custom inspectors, procgen tools from ch 6)
- 11.3 Content & Asset Pipeline Tools (importers, validators for naming/metadata from ch 10)
- 11.4 Build, CI/CD & Distribution (installers, launchers, patching)
- 11.5 Debug, Telemetry & Live Tools (AI debug from ch 8, analytics hooks)
- 11.6 Localisation & QA tooling
- 11.7 Tool Matrix (table: tool · purpose · owner · build vs buy · phase)
- Open Decisions box.

## Depth rule
D-11 = UNDECIDED → engine-specific rows say "engine-dependent".
