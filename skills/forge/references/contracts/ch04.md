# Contract — 4_Gameplay and Mechanics.md

<!-- gdd-forge chapter contract · one file per chapter · index: ../chapter-contracts.md · consistency rules: ../consistency-rules.md -->

## Conventions
- `brief:D-xx` — a decision field from `brief-schema.md`
- `ch N` — the finished markdown of chapter N (path given by the orchestrator)
- `tmpl` — the template skeleton you must follow (headings fixed, guidance comments removed in output); path given in your dispatch
- `check` — the checklist you self-check against before reporting; path given in your dispatch
- `data` — reference knowledge; the only allowed source of external facts
- **Depth rule** — how this chapter shrinks or grows based on triggers. An N/A chapter still opens with one short paragraph stating why it is minimal and which decision caused it, followed by any residual content. **A section removed by a Depth rule keeps its numbered heading**, followed by exactly one line: `N/A — <reason, ≤ 12 words>.` (dispatch rule 10) — e.g. `N/A — no narrative framing.` No `(D-xx)` tail. Never delete a numbered heading: downstream dispatches are sliced by heading number and a missing heading halts the pipeline.
- Output language for the body = `brief:D-12`. Headings keep the English chapter-number prefix (e.g. `# 4. Gameplay and Mechanics`) in every language.

## Owner
`gdd-mechanics-designer`

## Consumes
ch 3; `brief:D-03, D-09, D-20, D-30, D-32, D-43, D-44`

## Template / checklists / data
- tmpl: `04-gameplay-mechanics.md`
- check: `ch04-mechanics.md`, `ch00-structure.md`
- data: `game-design-patterns.md`; the `data/monetization/` slices D-06 selects, only if D-06 is F2P/hybrid (see `data/monetization/_index.md`)

## Sections
- 4.1 Core Loop (30-second loop → 5-minute loop → session loop, each as a numbered cycle with the *verb* the player performs)
- 4.2 Meta Loop & Progression (unlock structure, currencies, power/skill growth; retention hooks per D-09)
- 4.3 World Rules & Physics (what is always true; movement, collision, time, death/failure)
- 4.4 Mechanics Catalogue (one sub-section per mechanic: intent · rules · inputs · outputs · edge cases · tunables table)
- 4.5 Systems Interaction Map (mermaid graph of mechanics → systems → resources)
- 4.6 Objectives, Challenge & Puzzle Structure
- 4.7 Economy (sources/sinks table; if D-06 is F2P, mark which sinks are monetised — using only D-30 mechanisms)
- 4.8 Multiplayer Rules (only if D-20 ≠ None: modes, match flow, fairness, D-32 scale)
- 4.9 Game Options, Saving & Replay (difficulty, save model, NG+)
- 4.10 Balance Framework (target curves described qualitatively + which tunables drive them)
- 4.11 Feature list table (id · feature · pillar · priority · depends-on)
- Open Decisions box.

## Depth rule
D-20 = None → 4.8 becomes one line "Single-player only (D-20)". D-09 = Micro/Small → 4.2 shortened to one progression track.

## Hard rules
every mechanic must cite the pillar it serves (from ch 3 §3.2). Monetised sinks may only use mechanisms in D-30; anything else is a GAP.
